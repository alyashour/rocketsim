#ifndef RS_SCENE_FRAME_H
#define RS_SCENE_FRAME_H

typedef struct {
    // Position
    float x;
    float y;
    float z;

    // Color
    float r;
    float g;
    float b;
} SceneFrame;

#endif // RS_SCENE_FRAME_H
